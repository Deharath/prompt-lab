interface Props {
    model: string;
    onChange: (model: string) => void;
}
declare const ModelSelector: ({ model, onChange }: Props) => import("react/jsx-runtime").JSX.Element;
export default ModelSelector;
