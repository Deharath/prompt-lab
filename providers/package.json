{
  "name": "@prompt-lab/providers",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext .ts --max-warnings 0",
    "tsc": "tsc -p tsconfig.json --noEmit"
  }
}
