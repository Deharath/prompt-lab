{
  "name": "@prompt-lab/evaluator",
  "version": "0.0.0",
  "description": "Core evaluation logic for assessing AI model outputs and prompt performance",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist && rm -f tsconfig.tsbuildinfo",
    "build": "tsc --pretty false",
    "test": "vitest run --reporter=dot",
    "lint": "eslint . --max-warnings 0 --quiet",
    "tsc": "tsc -p tsconfig.lint.json --noEmit --pretty false"
  },
  "dependencies": {
    "@dqbd/tiktoken": "^1.0.0",
    "openai": "^4.104.0",
    "p-limit": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "vitest": "^3.2.4"
  }
}
