{
  "name": "@prompt-lab/evaluator",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint . --max-warnings 0",
    "tsc": "tsc -p tsconfig.lint.json --noEmit"
  },
  "dependencies": {
    "@dqbd/tiktoken": "^1.0.0",
    "openai": "^4.104.0",
    "p-limit": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "vitest": "^3.2.4"
  }
}
